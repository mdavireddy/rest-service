

name: Dummy

on:
  push:
    branches: [ "dev" ]

permissions:
  contents: read
env:
  SNOW_URL: ${{ secrets.SNOW_URL }}
  SNOW_TOKEN: ${{ secrets.SNOW_TOKEN }}
  SNOW_TOOLID: ${{ secrets.SNOW_TOOLID }}    

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: mdavireddy/sndevops:1.0.0
      options: --user node
    steps:
       - name : create change
         run: | 
           sndevopscli get change -p "{\"buildNumber\":9746175054,\"stageName\":\"ServiceNowDevOpsChange\",\"pipelineName\":\"mdavireddy%2Frest-service%2FJava+CI+with+Gradle\"}"

      
