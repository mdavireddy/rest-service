

name: Dummy

on:
  push:
    branches: [ "main" ]  

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Build with Maven 
      run: echo $HOME 
      
  test:
    name: test
    runs-on: ubuntu-latest 
    
    steps:  
        
      - name: Echo 
        run: exit 1
   
    
